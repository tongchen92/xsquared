{
  "name": "xsquared",
  "version": "0.1.0",
  "description": "OpenClaw plugin for creating, reviewing, and publishing high-quality X posts with Birdclaw.",
  "type": "module",
  "private": false,
  "bin": {
    "xsquared": "./dist/xsquared.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tongchen92/xsquared.git"
  },
  "homepage": "https://github.com/tongchen92/xsquared#readme",
  "bugs": {
    "url": "https://github.com/tongchen92/xsquared/issues"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dashboard": "npm run build --silent && node dist/xsquared.js dashboard",
    "trends": "npm run build --silent && node dist/xsquared.js trends",
    "generate": "npm run build --silent && node dist/xsquared.js generate",
    "strategy": "npm run build --silent && node dist/xsquared.js strategy",
    "list": "npm run build --silent && node dist/xsquared.js list",
    "check": "npm run build --silent && node dist/xsquared.js doctor",
    "prepare": "npm run build"
  },
  "dependencies": {
    "birdclaw": "^0.5.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3"
  }
}
